# Fourier-domain PTA Detection Statistic

This repository contains a Python implementation of a **Fourier-domain detection statistic** for Pulsar Timing Array (PTA) gravitational-wave data analysis.  
It reconstructs the pipeline of van Haasteren et al. (AEI), using optimal statistics and Imhof’s generalized χ² method to compute right-tail p-values.

---

## 📦 Requirements

- Python 3.10 or newer
- All dependencies listed in `requirements.txt`

You can install them automatically with:

```bash
pip install -r requirements.txt
